cmake_minimum_required(VERSION 3.6)

project(EngineeringWork)

set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++14 -Wall -g")

set(MINGW_PATH C:/mingw-w64/i686-6.2.0-posix-dwarf-rt_v5-rev1/mingw32/i686-w64-mingw32)
set(PROJECT_SOURCE_DIR ${CMAKE_SOURCE_DIR}/src)
set(PROJECT_LINK_LIBS libstrmiids.a)

set(PROJECT_VERSION_MAJOR 0)
set(PROJECT_VERSION_MINOR 1)
set(PROJECT_VERSION_PATCH 0)


configure_file(
        ${PROJECT_SOURCE_DIR}/cmake_init/AutoGeneratedVariables.h.in
        ${PROJECT_SOURCE_DIR}/cmake_auto_create/AutoGeneratedVariables.h
)


link_directories(${MINGW_PATH}/lib)


include_directories(
        ${MINGW_PATH}/include
        ${PROJECT_SOURCE_DIR}/cmake_auto_create
)

file(
        GLOB PROJECT_SOURCE_FILE
        ${PROJECT_SOURCE_DIR}/*.cpp
        ${PROJECT_SOURCE_DIR}/cam_device/*.cpp
        ${PROJECT_SOURCE_DIR}/propertie/*.cpp
        ${PROJECT_SOURCE_DIR}/initialize/*.cpp
)

add_executable(EngineeringWork ${PROJECT_SOURCE_FILE})

target_link_libraries(EngineeringWork ${PROJECT_LINK_LIBS})